const Header = () => {
    return (
        <header className="header">
            <input
                className=" new-todo"
                placeholder="What needs to be done?"
            />
        </header>
    )
}

export default Header;